#!/bin/bash


filename="$1"

while read -r line; do
    name="$line"
    if [ "$name" = "0" ]; then
        echo 0.0000000001
    else
        echo $name
    fi
done  < $filename > ${filename%.*}_new.txt